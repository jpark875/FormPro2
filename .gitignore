__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Pose model binaries are fetched by scripts/fetch_model.py, not committed.
models/*.task

# Captured sessions / recorded reference clips
data/recordings/
data/raw/
*.mp4
*.avi

# Reference corpora are gym- and lifter-specific data, and synthetic profiles must
# never be mistaken for recorded ground truth once they are in version control.
# Recordings are yours to keep or share deliberately: git add -f to track one.
data/reference/*.json
